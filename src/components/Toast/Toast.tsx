import { FC } from "react";
import { DefaultComponentProps } from "@Types/Types";
import './Toast.css';

interface ToastProps extends DefaultComponentProps {}

const Toast: FC<ToastProps> = ({
  // id, className, style, onClick, children
}) => {

  return (
    <></>
  )
}

export default Toast